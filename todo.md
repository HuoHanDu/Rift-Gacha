# 开发规划与进度

> 后续开发按本文顺序执行。每完成一项就把状态改为 `[x]`；**未达成「完成判据」不得勾选**。
> 规则实现以 `docs/RULES.md` 为准，数据口径以 `docs/DATA.md` 为准，分层约定以 `AGENTS.md` §4 为准。

## 进度总览

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| P0 | 环境与仓库 | ✅ 完成 |
| P1 | 数据流水线与快照 | ✅ 完成 |
| P2 | 随机引擎（`core/`）+ 单元测试 | ✅ 完成 |
| P3 | 展示层（页面 / 卡片 / 资料卡） | ✅ 完成 |
| P4 | 端到端验证与打磨 | ✅ 完成 |
| P5 | 部署文档与 README | ✅ 完成 |
| P6 | （v2 备选）移动端适配 | ☐ |
| P7 | 揭幕动画 + 双队分组显示 | ✅ 完成 |

---

## P0 环境与仓库

- [x] **P0.1 git 初始化**
  - `git init -b main`；`.gitignore`（`node_modules/`、`dist/`、`unpackage/`、`.probe/`、日志、编辑器目录）。
  - 初始提交已固化 `AGENTS.md`、`docs/**`、`todo.md`、工程骨架。另加 `.gitattributes` 统一 LF，避免 Windows 换行噪音。
  - **完成判据**：`git log --oneline` 有 1 条提交；`git status` 干净；`git ls-files` 中 `node_modules`/`.probe` 计数为 0。✅ 已验证

- [x] **P0.2 uni-app（Vue 3 + Vite）骨架**
  - 手建 `uni-preset-vue#vite` 布局：`index.html`、`vite.config.ts`、`src/{main.ts,App.vue,manifest.json,pages.json,uni.scss,static/}`。
  - 所有 `@dcloudio/*` 锁死 `3.0.0-5020620260917001`，`vite` 锁 `5.2.8`，`vite-plugin-uni` 同版本。
  - **完成判据**：`npm install` 成功（527 包）；`npm run dev:h5` 起在 `http://localhost:5173/`（HTTP 200，页面模块可编译）；`npm run build:h5` 产出 `dist/build/h5`。✅ 已验证

- [x] **P0.3 TypeScript + 测试链路**
  - `tsconfig.json`（strict + `@/*` 路径别名）、`src/env.d.ts`、`vitest.config.ts`（node 环境，只扫 `tests/**`）。
  - **完成判据**：`npm run typecheck`（vue-tsc）无输出无错；`npm run test` 18 个用例全绿。✅ 已验证

- [x] **P0.4 npm scripts**
  - **完成判据**：`dev:h5` ✅、`build:h5` ✅、`preview:h5` ✅（HTTP 200）、`test` ✅、`typecheck` ✅、`fetch:data` ✅（P1.1 后补验）。全部实跑通过。

---

## P1 数据流水线与快照

- [x] **P1.1 数据源常量与抓取**
  - `scripts/lib/sources.mjs`（5 个主源 + 2 个交叉校验源）；`scripts/fetch-data.mjs`（3 次重试、20s 超时、AbortSignal、纯 JSON 解析、原子写入）。
  - **完成判据**：`npm run fetch:data` 实跑成功，patch 16.18。✅

- [x] **P1.2 normalize —— 英雄与召唤师技能**
  - 英雄 173 条（含 `alias` 驱动的图标 URL）；技能按 `gamemode` 含「经典」过滤得 9 条。
  - **完成判据**：产物含 `4 闪现`、`11 惩戒`，不含 `32`/`39`/`13`。✅

- [x] **P1.3 normalize —— 装备分组**
  - 传说池 107（剔除 `4643` 及 5 个辅助任务升级件）、鞋 7 + 升级鞋 7 + 升级映射 7、通用出门装 8、打野蛋 3、辅助出门装 1 + 升级件 5。
  - 鞋池用显式 ID 列表 + `into` 字段双重认定。
  - **完成判据**：各池条数匹配；传说池无鞋子、无任务专属件（已在断言中覆盖）。✅

- [x] **P1.4 normalize —— 符文树**
  - 5 系；排顺序取自 CommunityDragon `perkstyles.json`（`rune_list2.js` 的排顺序与客户端不一致，且数字样式键会丢原顺序）；小符文三排同样以官方数据为准。
  - 发现并修掉的坑：符文文案的 HTML 被**实体编码过一层**，必须先解码再剥标签。
  - **完成判据**：5 系；每系基石 ≥ 3、系内小符文 3 排 × 3 个；小符文 3 排 × 3 个且第 1 排为 {适应之力, 攻击速度, 技能急速}；启迪「巧具」排含 `8306`。✅

- [x] **P1.5 完整性断言与原子写入**
  - 硬断言（结构性不变量）+ 软提示（随版本漂移的条数，只打印新旧差异）；失败保留旧快照并非 0 退出。
  - `src/core/types.ts`（全项目类型唯一出处）、`src/core/constants.ts`、`src/data/index.ts`（`DATA: DataBundle`）。
  - **完成判据**：连续两次 `fetch:data`，第一次无对比基线、第二次全部显示「未变」；`typecheck` 与 `build:h5` 均通过。✅

---

## P2 随机引擎（`core/`）+ 单元测试

> 本阶段**不碰 UI**。`core/` 不得 import Vue / uni-app / DOM，也不得直接 import `src/data/*.json`（数据以 `DataBundle` 注入）。

- [x] **P2.1 `core/types.ts` + `core/random.ts`**
  - `types.ts` 是全项目类型唯一出处（P1 已建）；`constants.ts` 放运行时常量与固定映射。
  - `random.ts`：`createRng`(mulberry32)、`pick`、`pickMany`（无放回）、`shuffle`、`pickFromGroups`、`createRandomSeed`。
  - **完成判据**：`tests/random.test.ts` 18 个用例通过。✅

- [x] **P2.2 `core/validate.ts` + `core/positions.ts`**
  - V1~V6（`NO_PLAYERS`/`TOO_MANY_PLAYERS`/`MISSING_TEAM`/`TOO_MANY_PER_TEAM`/`DUPLICATE_POSITION`/`NOT_ENOUGH_CHAMPIONS`）。
  - 随机分队改为**发牌算法**而非「洗牌切两半」：洗牌切两半在 10 人都指定位置时几乎必然撞车；发牌保证每队位置不重复，无解时返回可读错误。
  - **完成判据**：`tests/validate.test.ts` 11 个 + `tests/positions.test.ts` 12 个用例通过。✅

- [x] **P2.3 `core/champions.ts` + `core/spells.ts`**
  - 英雄全场去重（`usedHeroIds` 由编排层持有）；技能按位置与开关取池。
  - **完成判据**：`tests/generate.test.ts` 覆盖打野必带惩戒、开关开时非打野无惩戒、开关关时可出现惩戒、两技能不重复、多人英雄不重复。✅

- [x] **P2.4 `core/items.ts`**
  - 出门装分流（辅助展示件、打野蛋、8 件通用）、6 件成装（无放回）、鞋子（中路升级）。
  - **完成判据**：`tests/items.test.ts` 7 个用例，各 500 seed，覆盖全部池与全部鞋型。✅

- [x] **P2.5 `core/runes.ts`**
  - 主系 / 副系 / 小符文 / 海克斯科技闪现罗网约束。
  - **完成判据**：`tests/runes.test.ts` 10 个用例，3000 seed 扫描：无闪现阶段 8306 一次都不出现；有闪现阶段能抽到且主副系必含启迪；「巧具」排 3 个候选都能抽到。✅

- [x] **P2.6 `core/generate.ts` 编排**
  - 按 §8 固定顺序串起，返回 `GenerateResult`（错误以 `ok:false` 返回，不抛异常）。
  - **完成判据**：`tests/generate.test.ts` 15 个用例：同 seed 深比较相等、不传 seed 时返回的 seed 可复现、1/5/10 人（随机分队与预先分队）各 200 seed 全部满足不变量。✅
  - 全量：**73 个用例通过**，`typecheck` 与 `build:h5` 均通过。

---

## P3 展示层

- [x] **P3.1 输入区**
  - 模式（单队 1–5 / 双队 ≤10）、分队方式（随机 / 预先）、惩戒开关、每人的名字 + 位置（随机 + 5 个位置）+ 队伍；添加/移除玩家；错误就地提示。
  - 切到「双队 + 预先分队」时自动按 `ceil/floor` 预填队伍；切模式时裁剪人数。
  - **完成判据**：`tests/render.test.ts` 的首页用例渲染出全部控件与文案；dev server 下 `index.vue` 编译通过。✅

- [x] **P3.2 `BuildCard.vue` 单份结果卡片**
  - 排布对齐 `hexfuser.com`：表头（序号/名字/位置/队伍）→ 英雄 → 召唤师技能 → 出门装 → 成装 6 件 + 分隔 + 鞋子 → 符文分隔 → 主/副系 → 小符文。
  - **完成判据**：SSR 渲染测试断言卡片里出现英雄、2 个技能、出门装、6 件成装、鞋子、基石、主副系小符文、3 个小符文，且不出现 `undefined`。✅

- [x] **P3.3 悬停资料卡**
  - `IconChip.vue` 自带浮层，**纯 CSS `:hover`**，零 JS；内容取自快照。
  - **完成判据**：装备/技能/符文/小符文/英雄的资料卡都由同一个组件渲染，`desc`/`meta` 均已接线。⚠️ 实际悬停手感需要人眼确认（SSR 测不出来）。
  - 遗留：v1 只支持鼠标悬停，移动端改点击展开列入 P6。

- [x] **P3.4 响应式与视觉**
  - 设计令牌集中在 `App.vue`（黑钢底 + 单点黄铜 `#c8973f`）；结果网格 `repeat(auto-fill, minmax(336px, 1fr))`；≤720px 收成单列、输入行换行。
  - 图标 `@error` 兜底为首字占位块。
  - **完成判据**：`build:h5` 成功；样式为纯 CSS 无外部依赖。⚠️ 三档宽度的实际观感需要人眼确认。

- [x] **P3.5 合规声明**
  - 页脚写明数据来源、「非官方」声明与当前补丁版本；页头显示补丁徽标。
  - **完成判据**：`render.test.ts` 断言「非官方娱乐工具」与「补丁 16.18」出现在页面上。✅

---

## P4 端到端验证与打磨

- [x] **P4.1 全量测试与构建**
  - **完成判据**：`npm run test` 78 个用例全绿（7 个文件）；`npm run typecheck`（vue-tsc）无输出；`npm run build:h5` 成功产出 `dist/build/h5`。✅

- [x] **P4.2 人工核对清单**
  - 新增 `npm run sample`（`scripts/dump-sample.ts`，走 vite-node），把结果打印成人能读的文本，逐条对照 `docs/RULES.md` §10 核对。
  - 已核对的样本：`-- 1 --seed 42`、`-- 10 --seed 20260921`。核对到的证据：
    - 上单：闪现 + 传送，无惩戒；出门装多兰之弓（通用池）；鞋子轻灵之靴（未升级）
    - 辅助：云游图鉴 → 展示为 扎兹沙克的溃口 / 圆梦使者；鞋子未升级
    - 打野：惩戒 + 屏障；出门装 风行狐幼体
    - 中单（#6 / #8）：鞋子为 猩红明朗 / 带链碾碎者（**升级款**）；非中路全部为未升级款
    - 副系恒与主系不同；主系 3 个小符文分属 3 个不同排（如 气定神闲 / 传说：欢欣 / 致命一击）
    - 副系 2 个小符文分属 2 个不同排
    - 小符文恒为 进攻 / 灵活 / 防御 各 1
    - 10 人双队：蓝红各 5 人，每队位置互不重复；英雄 10 个互不重复
  - **完成判据**：以上每条都在真实输出里出现过。✅
  - ⚠️ 悬停手感、三档宽度的观感、真实浏览器里的交互需要人眼看，本机无法截图验证。

- [x] **P4.3 生产产物验证**
  - `npm run serve:dist`（自带零依赖静态服务器）：`/` 与 `/assets/*.css` 均 200，未知路径回退入口页。
  - `npm run preview:h5`：`/` 200。
  - 产物内联检查：主 bundle（181 KB）里能搜到「无尽之刃」「海克斯科技闪现罗网」「风行狐幼体」「云游图鉴」「Annie」，确认运行时不请求数据 JSON。
  - 子路径：改 `h5.router.base = /lol-random/` 后重构建，产物资源路径变为 `/lol-random/assets/...`，验证后已还原 manifest。
  - **完成判据**：以上全部实跑通过。✅

---

## P5 部署文档与 README

- [x] **P5.1 `docs/DEPLOY.md`**
  - 覆盖产物结构、本机验证方式、nginx 配置、子路径部署、Docker、静态托管、数据刷新与发版、常见问题、运维要点。
  - 附带 `Dockerfile`、`nginx.conf`、`.dockerignore`、`scripts/serve-dist.mjs`。
  - **完成判据**：文档开头明确区分「本机已验证」与「未在本机验证（nginx / Docker，附原因）」；所有本机可执行的命令均已实跑成功。✅

- [x] **P5.2 `README.md`**
  - 项目简介、玩家视角的规则速览、常用命令表、目录说明、技术栈、部署指引、声明。
  - **完成判据**：只有仓库的情况下照 README 即可跑起 dev server（命令与 `package.json` 脚本一一对应）。✅

---

## P6 （v2 备选）移动端适配

- [ ] 断点与触控交互（悬停资料卡改点击）、`manifest.json` 平台配置、真机/模拟器验证。
- **完成判据**：移动端构建产物在至少一台真机上可完成一次完整随机。

---

## P7 揭幕动画 + 双队分组显示（2026-09-22）

需求：双队随机分队后要能按队伍排序显示；新增一个**默认关闭**的揭幕动画开关；动画按段播放且**同一段内的格子同时停**；点击画面快进当前段，右上角可跳过全部。

- [x] **P7.1 `src/reveal/sections.ts` —— 分幕剧本定义**
  - 分幕顺序固定为 位置 → 英雄 → 召唤师技能 → 出门装 → 成装+鞋子 → 符文；每幕帧数与「先快后慢」的帧间隔是纯数据/纯函数。
  - **完成判据**：`tests/reveal.test.ts` 断言帧间隔单调不降、单幕 0.3~1.2s、单玩家总时长 2~6s。✅

- [x] **P7.2 `src/reveal/plan.ts` —— 结果转轮盘**
  - 每个格子生成一串候选，**末项必是真实值**，中途的陪跑项不得等于真实值（否则提前剧透）。陪跑项用同一个 seed 派生，所以动画本身也可复现。
  - 出门装/鞋子的候选池按位置分流，和真实随机一致。
  - **完成判据**：断言格子数量与帧数正确、每格末帧等于真实结果、扫 40 个 seed 无提前剧透、同 seed 剧本完全一致。✅

- [x] **P7.3 `src/reveal/controller.ts` —— 播放器状态机**
  - 一次只有一幕滚动；一幕播完接下一幕，一名玩家播完接下一位；`finishCurrent()` 定格当前幕并继续；`skipAll()` 全部定格；`dispose()` 清定时器。
  - 支持自定义播放顺序，双队分组后按**显示顺序**播，避免卡片乱跳。
  - **完成判据**：18 个用例，含「任意时刻最多只有一段在滚动」「快进可一路推到终点」「跳过之后定时器不再改状态」「dispose 后不再有状态变化」。✅

- [x] **P7.4 卡片与图标支持三态**
  - `BuildCard` 按 `hidden / rolling / done` 渲染：隐藏时只留序号与玩家名、格子是呼吸占位块；滚动时显示当前帧；定格后就是最终结果。
  - `IconChip` 新增 `rolling`：滚动中不出资料卡（避免悬停看到未揭晓内容），并压暗以示区分。
  - **完成判据**：SSR 渲染测试断言「全部隐藏时不泄露英雄/装备/符文名」「滚动中出的是轮盘帧而非最终值」「全部定格后与不用动画渲染逐字节相同」「滚动中不出现 `chip__tip`」。✅

- [x] **P7.5 首页接线**
  - 「揭幕动画：开/关」默认**关**；「按队伍排序 / 按输入顺序」仅双队模式出现，默认按队伍。
  - 右上角「跳过动画」按钮（仅播放中出现）；结果区整体可点击快进；播放中的卡片加铜色高亮并自动滚进视野。
  - **完成判据**：`typecheck` 通过、`build:h5` 通过、dev server 下所有新模块编译 200。✅
  - ⚠️ 实际播放手感（快慢是否舒服、占位块呼吸是否顺眼）需要人眼看，本机无法截图验证。

- [x] **P7.6 段间停顿（2026-09-22 追加）**
  - 原本一幕定格后立刻开下一幕，看起来像连续抖动而不是「一格一格揭晓」。现在每两幕之间停 `SECTION_GAP_MS = 170ms`，换人时停 `PLAYER_GAP_MS = 340ms`。
  - 这一拍里**没有任何一幕在滚动**，所以视觉上就是承接关系；点击画面会**掐掉**这一拍立刻开下一段，手动快进的段间也不留停顿（点下去就该有反应）。
  - 实测节奏：位置 488ms / 英雄 846ms / 技能 608ms / 出门装 488ms / 成装 846ms / 符文 725ms，段间共 850ms ⇒ **单人 4.85s**，10 人约 51.5s（可点画面逐段快进或一键跳过）。
  - **完成判据**：新增 8 个用例（第一幕不等、定格后确有空档、空档未满不提前开转、空档满后开转、整段播放期间确实存在 5 个空档、换人停顿更长、停顿期点击立刻开下一段、手动快进不留停顿、跳过/销毁掐掉未到期停顿）；`playerDuration()` 已含段间停顿。✅

- 全量：**112 个用例通过**（8 个文件）。

---

## 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-21 | 初始化：需求整理、架构设计、规则规范、数据口径，确认 10 项决策 |
| 2026-09-22 | P0–P5 完成并部署到 `lol.huohandu.cn`；v1 优化五则（去 `descHtml`、资料卡对齐、IME、键盘可达性、符文详情） |
| 2026-09-22 | P7：揭幕动画（默认关闭，可点击快进/跳过）+ 双队按队伍分组显示 |
