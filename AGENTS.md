# 峡谷全随机构筑器（LoL Full Random Build Generator）

一个用于《英雄联盟》召唤师峡谷的娱乐向随机构筑生成器：输入人数与位置，一键随机出**位置、英雄、召唤师技能、出门装、六件成装、鞋子、主副系符文与详细点法、三个小符文**。

参考形态：<https://hexfuser.com/zh-cn>（`虚空之女的混乱理论` 卡片排布）。
数据来源：<https://101.qq.com/#/rankings/rift>、<https://101.qq.com/#/data/items>（底层为 `game.gtimg.cn` CDN 静态数据）。

---

## 一、技术栈与交付范围

| 项 | 决定 |
| --- | --- |
| 框架 | uni-app（Vue 3 + Vite），**H5/Web 端为第一代交付目标** |
| 语言 | TypeScript |
| 样式 | 原生 CSS + `rpx`/`px` 混合，响应式，扁平化，简洁现代 |
| 后端 | **v1 无运行时后端**：纯静态 SPA + 构建期 Node 数据脚本 |
| 移动端 | 复用同一份 uni-app 代码，v1 不做专项适配，仅保证不崩 |

架构、模块边界与未来接后端的接缝见 `docs/ARCHITECTURE.md`。
数据源、快照格式与刷新流程见 `docs/DATA.md`。
**随机规则的实现权威规范见 `docs/RULES.md`**（本文只描述需求意图，冲突时以 `docs/RULES.md` 为准）。
开发顺序与进度见 `todo.md`。

---

## 二、需求（用户原始需求整理）

### 2.1 输入区

1. **人数**：可选，1~N（单队上限 5）。
2. **玩家名字**：每个玩家一个，选填。
3. **位置**：上单 / 打野 / 中单 / 下路 / 辅助，每个玩家选填。
   - 人数与位置数量必须相等（选了 N 个人就必须正好 N 个位置）。
   - 每个玩家只能选一个位置，或留空交给随机；同一场内位置不重复。
4. **两队模式**：可输入最多 10 个名字。
   - 不预先分队 → 随机分成两队，每队 5 人。
   - 预先分队 → 按指定分队处理。
   - **每个玩家都可预先指定位置（选填），未指定的随机补齐，每队 5 个位置互不重复。**
5. 允许**直接点「开始随机」**：不填任何输入时等价于单人生成。

### 2.2 随机输出（依次产生）

位置 → 英雄 → 两个召唤师技能 → 出门装 → 六件成装 + 一双鞋 → 主系符文（含基石）→ 副系符文 → 符文详细点法 → 三个小符文。

- **单人随机**：输出 1 份结果。
- **多人随机**：分别输出多份结果，每份的排布参考 `hexfuser.com` 的卡片形态。
- 每个结果固定为 **6 件传说成装 + 1 双鞋**（见「三、已确认决策」第 1 条）。

### 2.3 视觉与交互

- 响应式布局，**简洁、现代、扁平化**，不做过度设计。
- 鼠标悬停图片时弹出资料卡（英雄/装备/符文/召唤师技能的说明），数据优先取自已抓取的官方数据快照，不额外请求第三方接口。

### 2.4 揭幕动画（可开关，默认关闭）

打开后点「开始随机」，结果**按玩家逐个揭晓**：先只显示 `01 玩家N` 这类标识、格子全部留空，然后依次播放。

- 每位玩家内部的揭晓顺序固定为：**位置 → 英雄头像 → 两个召唤师技能 → 出门装 → 成装 + 鞋子 → 符文**。
- **同一段内的格子同时开转、同时停下**（两个召唤师技能一起滚动），因为它们共用一条时间线。
- 表现形式是老虎机式的轮盘滚动，只在格子内滚，先快后慢，最后一帧定格在真实结果上。
- **点击画面**让当前这一段立刻定格并接着播下一段；**右上角「跳过动画」**跳过全部，等同没开动画。
- 多张卡片的播放顺序跟随**显示顺序**（双队分组后是蓝队在前），播到哪张卡就把它滚进视野。

### 2.5 附加交付物

- 一份**服务器部署文档**（`docs/DEPLOY.md`）。

---

## 三、已确认决策（2026-09-21 与用户确认，不可擅自更改）

1. **装备栏构成统一为 6 件传说成装 + 1 双鞋**，所有位置一致；下路不再单独多加一个格子。
2. **数据策略**：构建期抓取生成**数据快照**打进项目，另附刷新脚本；装备/英雄/符文/技能图标**热链 `game.gtimg.cn` CDN**，不落盘图片。
3. **v1 不做运行时后端**，纯静态前端 + 构建期 Node 数据脚本。
4. **小符文三排**按官方最新数据：进攻=适应之力/攻击速度/技能急速；灵活=适应之力/移动速度/成长生命值；防御=生命值/韧性和减速抗性/成长生命值。
5. **副系必须与主系不同**；从副系 3 排小符文中随机选 **2 排**，每排随机 1 个（共 2 个）。
6. **海克斯科技闪现罗网**：主系或副系为启迪都算「随机到了启迪系」；若「巧具」排被选中但条件不满足，则**在该排剩余 2 个符文中重随**（其余已随机结果保留）。
7. **惩戒开关默认开启**（非打野位置不会随机到惩戒），页面上可关闭。
8. **十人双队模式**：每个玩家都可预先指定位置（选填），未指定的随机补齐，每队 5 个位置互不重复。
9. **去重范围**：英雄全场所有玩家不重复；装备/召唤师技能/符文仅同一玩家内不重复，跨玩家可重复。
10. **传说装备池**：剔除 6 件任务专属件（`4643` 警觉眼石、`3869/3870/3871/3876/3877` 辅助任务升级件），其余 107 件全部进池（含 `3041` 梅贾的窃魂卷）。

---

## 四、项目约定（写代码时必须遵守）

### 4.1 分层铁律

- `src/core/` 是**纯逻辑层**：随机引擎、池子构造、规则校验。**不得**导入任何 uni-app API、DOM API 或 Vue 运行时。它必须能在 Node 里直接跑，因此可以被单元测试覆盖。
- `src/reveal/` 是**揭幕动画层**：轮盘剧本（纯数据 + 纯函数）与播放器状态机。剧本与状态机同样不依赖 DOM；只有拿到真实结果后才构建，**不参与也不影响随机结果**。
- `src/data/` 是**构建期生成物**，由 `scripts/fetch-data.mjs` 写入。**手改会被下次刷新覆盖**；数据口径的修正要改脚本，不改变量。
- `src/components/`、`src/pages/` 只做渲染与交互，不承载随机规则。
- 新增规则 → 改 `src/core/` + 补 `tests/` 用例；改数据口径 → 改 `scripts/`；改展示 → 改 `src/components/`；改动画 → 改 `src/reveal/`。

### 4.2 随机引擎纪律

- 所有随机必须走 `src/core/random.ts` 的**可注入种子 RNG**（默认 `Math.random`）。禁止在组件里直接写 `Math.random()`。
- 种子可复现：同一个 seed + 同一份快照 + 同一份输入 ⇒ 同一份结果。这是回归测试和「复现某次随机」的基础。
- 每个玩家的生成互不依赖顺序：多人结果由「输入列表 + 全局去重集」决定，不由生成顺序决定语义。

### 4.3 验证纪律

- 声称「规则实现完成」前，必须在 `tests/` 里有对应用例并通过 `npm run test`。
- 声称「页面可用」前，必须实际构建成功（`npm run build:h5`）并启动 dev server 看过页面。
- 规则类断言要测**边界**，不只测正常路径：打野必带惩戒、非打野不带惩戒（开关开时）、六件成装无重复、副系≠主系、海克斯闪现罗网的触发条件、多人英雄不重复。

### 4.4 数据与版权

- 只使用公开 CDN 的静态数据（`game.gtimg.cn`）与官方符文数据（CommunityDragon）作交叉校验。
- 项目为非商业娱乐用途；页面上标注数据来源与「非官方」声明。
- 图片一律热链，不把 Riot/腾讯的美术资源打包进仓库。

### 4.5 命名与文案

- 代码标识符英文，UI 文案与注释中文。
- 领域术语统一用 `docs/RULES.md` 的词汇表（位置/分路、基石、主系、副系、小符文、成装、出门装）。

---

## 五、常用命令

```bash
npm run dev:h5         # 启动 Web 开发服务器（http://localhost:5173）
npm run build:h5       # 构建 Web 产物到 dist/build/h5
npm run serve:dist     # 零依赖静态服务器，预览构建产物（:4180）
npm run preview:h5     # vite preview 预览构建产物（:4173）
npm run test           # 跑随机引擎与渲染的单元测试
npm run typecheck      # TypeScript 类型检查
npm run fetch:data     # 抓取并生成 src/data/*.json 快照
npm run sample -- 10   # 在终端打印一次 10 人随机结果，用来核对规则
```

## 六、当前进度

`todo.md` 的 P0–P7 已完成（环境 / 数据 / 引擎 / 展示 / 验证 / 文档 / 动画与排序）。改动前先看 `todo.md` 的「进度总览」，
新增规则请同步补 `tests/` 用例与 `docs/RULES.md` 对应小节。

**线上地址**：<https://lol.huohandu.cn/>（发布流程与回滚见 `docs/DEPLOY.md` 第 1 节）。
